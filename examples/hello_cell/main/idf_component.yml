dependencies:
  # Runtime backend for the public edge kit. WAMR is published as
  # `espressif/wasm-micro-runtime`; the old wasm3 backend is retained only as
  # a reference port and is not part of the active build.
  espressif/wasm-micro-runtime: "^2.4.0"
  idf: ">=5.0"
