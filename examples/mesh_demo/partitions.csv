# mesh_demo — custom partition table.
# Default IDF single-app layout has a 1 MB app partition; that was tight
# (~6% free) with the lightbulb-channel deck embedded, and adding the
# cell-engine + WAMR pushes us well over. The XIAO ESP32-C6 has 4 MB of
# flash, so we bump to 4 MB flash + a 2 MB app partition. Plenty of room
# for WAMR (~250 KB) + the 29 KB WASM cell-engine + the deck.
# Name,    Type, SubType, Offset,   Size,    Flags
nvs,       data, nvs,     0x9000,   0x6000,
phy_init,  data, phy,     0xf000,   0x1000,
factory,   app,  factory, 0x10000,  0x200000,
