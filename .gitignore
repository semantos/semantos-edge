# ESP-IDF build output
build/
managed_components/
sdkconfig
sdkconfig.old
dependencies.lock

# Zig
.zig-cache/
zig-out/

# Node / Bun (TS tools)
node_modules/
bun.lockb

# Local config / secrets
.env
.env.*
!.env.example

# Holds a spend key (WIF) for an unsettled UTXO — never commit
.gated-spend-state.json

# OS
.DS_Store
tools/fleet-zig/spike/.zig-cache/
tools/fleet-zig/spike/zig-out/
tools/fleet-zig/.zig-cache/
tools/fleet-zig/zig-out/
