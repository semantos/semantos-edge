# ESP-IDF build output
build/
managed_components/
sdkconfig
sdkconfig.old
dependencies.lock

# Zig
.zig-cache/
zig-out/

# Node / Bun (TS tools)
node_modules/
bun.lockb

# Holds a spend key (WIF) for an unsettled UTXO — never commit
.gated-spend-state.json

# OS
.DS_Store
